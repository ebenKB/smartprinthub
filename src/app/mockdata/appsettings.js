// Do not scope this to any company
// These are application level settings and they affect

const settings = {
  jobChargeRate: 0.025, // the rate to charge the user for a single job
};

export default settings;
