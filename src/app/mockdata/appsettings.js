// Do not scope this to any company
// These are application level settings and they affect

const settings = {
  paymentChargeRate: 0.02,
  jobChargeRate: 0.01, // the rate to charge the user for a single job
};

export default settings;
